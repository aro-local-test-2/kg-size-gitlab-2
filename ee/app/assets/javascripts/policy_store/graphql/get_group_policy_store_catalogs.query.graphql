# @feature_category: security_policy_management

query getGroupPolicyStoreCatalogs($fullPath: ID!) {
  group(fullPath: $fullPath) {
    id
    policyStore {
      triggers {
        id
        name
      }
      rules {
        id
        name
      }
      actions {
        id
        name
      }
    }
  }
}
