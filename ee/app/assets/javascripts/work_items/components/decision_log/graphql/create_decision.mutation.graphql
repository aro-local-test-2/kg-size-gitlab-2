#import "./decision.fragment.graphql"

# @feature_category: team_planning
mutation workItemDecisionCreate($input: WorkItemDecisionCreateInput!) {
  workItemDecisionCreate(input: $input) {
    decision {
      ...Decision
    }
    errors
  }
}
