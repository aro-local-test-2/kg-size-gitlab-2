#import "./decision.fragment.graphql"

# @feature_category: team_planning
mutation workItemDecisionArchive($input: WorkItemDecisionArchiveInput!) {
  workItemDecisionArchive(input: $input) {
    decision {
      ...Decision
    }
    errors
  }
}
