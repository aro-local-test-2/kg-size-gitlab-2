#import "./decision.fragment.graphql"

# @feature_category: team_planning
mutation workItemDecisionUpdate($input: WorkItemDecisionUpdateInput!) {
  workItemDecisionUpdate(input: $input) {
    decision {
      ...Decision
    }
    errors
  }
}
