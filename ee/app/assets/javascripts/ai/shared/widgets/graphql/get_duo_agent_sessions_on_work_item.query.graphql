# @feature_category: duo_agent_platform
#import "./duo_agent_session.fragment.graphql"

query getDuoAgentSessionsOnWorkItem($id: WorkItemID!, $first: Int = 20) {
  workItem(id: $id) {
    id
    features {
      aiSession {
        duoWorkflows(first: $first) {
          pageInfo {
            hasNextPage
          }
          nodes {
            ...DuoAgentSession
          }
        }
      }
    }
  }
}
